version: "3.8"
services:
  simulator:
    build: .
    container_name: forwardfill-realtime
    env_file:
      - ./.env
    volumes:
      - ./:/app
    restart: unless-stopped

  # Optional local mosquitto for testing. Uncomment if you want a local broker.
  # mosquitto:
  #   image: eclipse-mosquitto:2.0
  #   container_name: mosquitto-local
  #   ports:
  #     - "1883:1883"
  #   volumes:
  #     - ./mosquitto/config:/mosquitto/config
  #     - ./mosquitto/data:/mosquitto/data
  #     - ./mosquitto/log:/mosquitto/log
  #   restart: unless-stopped
